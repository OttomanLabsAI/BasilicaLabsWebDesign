{
  "name": "basilicalabswebdesign",
  "version": "2.8.0",
  "private": true,
  "description": "FKarim Web Design - the sales site for the web design service. Cloudflare Workers static assets, no build step.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
