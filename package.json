{
  "name": "basilicalabswebdesign",
  "version": "2.9.0",
  "private": true,
  "description": "Built by Fid - the sales site for Fadil Karim's design studio. Cloudflare Workers static assets, no build step.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
